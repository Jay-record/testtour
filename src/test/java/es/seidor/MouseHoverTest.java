package es.seidor;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;
import org.testng.annotations.Test;

import java.util.List;

public class MouseHoverTest extends TestMethods{
    @Test
    public void Test() {
     /*  webDriver.get("https://www.tous.com/es-es/pulsera-silueta-de-plata-vermeil/p/913561550");
        Actions action = new Actions(webDriver);
        List<WebElement> topBar = webDriver.findElements(By.className("displayAuxiliar"));
        List<WebElement> bottomBar = webDriver.findElements(By.className("displayAuxiliar").findElements(By.className("sub-catergories-text")));
        for(int i =0 ; i< topBar.size();i++) {
          /*  System.out.println(webDriver.findElements(By.className("displayAuxiliar")).get(i).getText());
            WebElement button = webDriver.findElements(By.className("displayAuxiliar")).get(i);
            action.moveToElement(button).perform();
            pause(1);
            for(int k = 0 ; k< bottomBar.size(); k++){
                System.out.println(webDriver.findElements(By.className("displayAuxiliar").findElements(By.className("sub-catergories-text"))).get(i).getText());
                button = webDriver.findElements(By.className("displayAuxiliar").findElements(By.className("sub-catergories-text"))).get(i).getText();
                action.moveToElement(button).perform();
            }

            webDriver.findElements(By.className("displayAuxiliar").findElements(By.className("sub-catergories-text"))).get(i).getText();
        }
        pause(3);*/
    }
    }
